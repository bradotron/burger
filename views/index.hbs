<div class="container my-4">
  <div class="jumbotron">
    <h1>{{title}}</h1>
    <hr class="my-4">
    <p>Add burgers to by entering a name and clicking 'Add Burger'. Then devour burgers by clicking 'Devour'.</p>
  </div>
  <div class="container">
    <div class="row">
      <div class="col-sm">
        {{!-- undevoured burgers go here --}}
        <div class="card ">
          <div class="card-header">
            Fresh Meat!
          </div>
          <div class="card-body">
            {{#each burgers}}
              {{#if this.devoured}}
              {{else}}
              <div class="card">
              <p class="mx-auto my-1">{{this.burger_name}}</p>
              <button class="devour-btn mx-auto my-1" data-burgerId="{{this.id}}">Devour</button>
              </div>
              {{/if}}
            {{/each}}
          </div>
        </div>
      </div>
      <div class="col-sm">
        {{!-- devoured burgers go here --}}
        <div class="card ">
          <div class="card-header">
            Sold Out!
          </div>
          <div class="card-body">
            {{#each burgers}}
              {{#if this.devoured}}
              <div class="card">
                <p class="mx-auto my-auto">{{this.burger_name}}</p>
              </div>
              {{else}}
              {{/if}}
            {{/each}}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>